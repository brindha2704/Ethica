{
  "name": "ethica-root",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd ethica-frontend && npm install",
    "build": "cd ethica-frontend && npm run build && cd .. && mkdir -p public && cp -rp ethica-frontend/dist/* public/"
  }
}
