{
  "name": "ethica-root",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd ethica-frontend && npm install",
    "build": "export UV_LINK_MODE=copy && cd ethica-frontend && npm run build && cd .. && mkdir -p public && cp -rv ethica-frontend/dist/* public/"
  }
}
